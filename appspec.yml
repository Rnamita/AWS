version: 0.0
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
hooks:
  BeforeInstall:
    - location: AWS/beforeInstall
      timeout: 300
      runas: ubuntu
  AfterInstall:
    - location: AWS/AfterInstall
      timeout: 300
      runas: ubuntu
